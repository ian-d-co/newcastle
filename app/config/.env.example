# Dicksord Fest 2026 - Newcastle Event Management System
# Environment Configuration Template
# 
# Copy this file to .env in the project root and update with your actual values
# DO NOT commit the actual .env file to version control

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Environment: development, staging, or production
APP_ENV=production

# Debug mode: true or false (NEVER enable in production!)
APP_DEBUG=false

# Application URL (without trailing slash)
APP_URL=https://yourdomain.com

# Timezone (PHP timezone identifier)
TIMEZONE=Europe/London

# =============================================================================
# DATABASE CONFIGURATION (Hostinger Example)
# =============================================================================

# Database host (usually 'localhost' for Hostinger shared hosting)
DB_HOST=localhost

# Database port (usually 3306, can be omitted for default)
DB_PORT=3306

# Database name (assigned by Hostinger, format: u[userid]_[dbname])
# Example: u983097270_newc
DB_NAME=u983097270_newc

# Database username (same as database name prefix on Hostinger)
DB_USER=u983097270_newc

# Database password (set during database creation)
DB_PASSWORD=your_secure_database_password_here

# =============================================================================
# SESSION CONFIGURATION
# =============================================================================

# Session name (used for session cookie name)
SESSION_NAME=dicksord_fest_2026

# Session lifetime in seconds (3600 = 1 hour, 7200 = 2 hours)
SESSION_LIFETIME=3600

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# CSRF token field name
CSRF_TOKEN_NAME=csrf_token

# =============================================================================
# HOSTINGER SPECIFIC NOTES
# =============================================================================
# 
# 1. Database credentials can be found in:
#    - Hostinger Control Panel → Databases → MySQL Databases
#    - Look for the database you created for this project
# 
# 2. The database host is typically 'localhost' for shared hosting
# 
# 3. Database name format: u[userid]_[your_chosen_name]
#    Example: u983097270_newc (where 983097270 is your user ID)
# 
# 4. Username is usually the same as the database name
# 
# 5. For SSL/HTTPS:
#    - Hostinger provides free SSL certificates
#    - Enable SSL in the control panel
#    - Update APP_URL to use https://
# 
# 6. File permissions:
#    - .env file should be chmod 600 (read/write for owner only)
#    - Other PHP files: chmod 644
#    - Directories: chmod 755
# 
# =============================================================================
# EXAMPLE VALUES FOR LOCAL DEVELOPMENT
# =============================================================================
# 
# APP_ENV=development
# APP_DEBUG=true
# APP_URL=http://localhost
# DB_HOST=localhost
# DB_NAME=newcastle_dev
# DB_USER=root
# DB_PASSWORD=
# 
# =============================================================================
